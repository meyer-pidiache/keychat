services:
  keychat:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - PORT=8080
      - DB_PATH=/data/keychat.db
      - WEB_DIR=/web
    volumes:
      - keychat-data:/data

volumes:
  keychat-data:
